@page "/wizard"
@attribute [AllowAnonymous]

@* Wave 1 PR-F0 placeholder. PR-F1 ships the WizardShell + layout chrome.
   PR-F2 ships WizardAnswerStream + the SSE endpoint + /api/wizard/ask:stream.
   ADR-0026 § 1 locks /wizard as the primary Wizard surface (anonymous). *@

<PageTitle>PinballWizard — Ask the Wizard</PageTitle>

<MudContainer MaxWidth="MaxWidth.Medium" Class="mt-8">
    <MudText Typo="Typo.h4">Wizard placeholder — PR-F1 ships layout shell.</MudText>
</MudContainer>
