@namespace PinballWizard.Web.Components.Shared

<MudCard Elevation="2" Class="@Class">
    <MudCardContent>
        <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
            <MudIcon Icon="@Icon" Color="@IconColor" Size="Size.Large" />
            <MudStack Spacing="0">
                @Content
                <MudText Typo="Typo.body2" Color="Color.Secondary">@Label</MudText>
                @if (Caption is not null)
                {
                    <MudText Typo="Typo.caption" Color="Color.Secondary">@Caption</MudText>
                }
            </MudStack>
        </MudStack>
    </MudCardContent>
    <MudCardActions>
        <MudButton Href="@ActionHref" Variant="Variant.Text" Color="@ButtonColor" Size="Size.Small">
            @ActionLabel
        </MudButton>
    </MudCardActions>
</MudCard>

@code {
    [Parameter, EditorRequired] public string Icon { get; set; } = null!;
    [Parameter, EditorRequired] public Color IconColor { get; set; }
    // CTA color is decoupled from the decorative icon color and defaults to the
    // amber interactive role. Icons may be neutral/expressive; the action is always amber.
    [Parameter] public Color ButtonColor { get; set; } = Color.Primary;
    [Parameter, EditorRequired] public string Label { get; set; } = null!;
    [Parameter, EditorRequired] public string ActionHref { get; set; } = null!;
    [Parameter, EditorRequired] public string ActionLabel { get; set; } = null!;
    [Parameter, EditorRequired] public RenderFragment Content { get; set; } = null!;
    [Parameter] public string? Caption { get; set; }
    [Parameter] public string Class { get; set; } = "admin-summary-card";
}
