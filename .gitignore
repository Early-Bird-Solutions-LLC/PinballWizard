# Build output
bin/
obj/
publish/

# IDE / editor
.vs/
.vscode/
.claude/
# Project-scoped skills + commands are checked in (they apply for every
# contributor); everything else under .claude/ — runtime history,
# scheduled-task locks, machine-local settings — stays ignored.
!.claude/skills/
!.claude/skills/**
!.claude/commands/
!.claude/commands/**
*.user
*.suo
*.DotSettings.user
.idea/

# Data (Docker volume — not checked in)
data/

# OS
.DS_Store
Thumbs.db

# Bicep — local parameter overrides (never committed; deploy script auto-detects)
*.local.bicepparam
